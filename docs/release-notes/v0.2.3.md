# ChatGPT Account Keeper 0.2.3

## 更新恢复

- 修复 Agent 因数据库版本不兼容而无法启动时，应用内更新也随之失败的问题。
- Agent 离线时可直接进入安全更新流程，无需先启动旧版 Agent。
- 下载和安装期间独占数据目录并抑制后台重连，避免 Agent 并发启动或数据库被旧版本访问。
- 兼容损坏或残缺的诊断锁文件，同时继续阻止仍在运行或无法确认状态的旧 Agent。

## 本次发布说明

- 本版经发布者明确授权，不进行平台代码签名，也不下载发布产物到本地验收；保留 Tauri 更新签名、四平台原生构建、Agent 烟测、SBOM 和校验和检查。
