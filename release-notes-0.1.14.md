## 登录与注册

- 修复输入邮箱验证码后返回 400、随后反复进入人机验证的问题。交互式 Chrome 不再向网页暴露自动化标记。

## 账号运行与调度

- 修复绑定分组代理的账号被统一误判为“代理节点不可用”，立即运行现在会读取真实节点状态。
- 修复自动任务完成后未正确记录结果或安排下一次运行，以及重启后排期显示不一致的问题。
- 调度停止、账号停用或删除不再被错误记录为一次运行失败。
